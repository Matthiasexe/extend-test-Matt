// const javaExercise = [
//   1,
//   3,
//   9,
//   6,
//   22,

// ]

// const javaFunction = (arg) => { return arg % 3 === 0 } // returns list of T or F if number is divisible by 3
// const javaBool = javaExercise.map(javaFunction)
// console.log(javaBool)

// const javaEven = (buns) => { return buns % 2 === 0 ? 'Even' : 'odd' } // J ex2
// const JsExcercise2 = (numArray) => { return numArray.map(javaEven) }
// const JsExcercise2 = (numArray) => { return numArray.map((buns) => { return buns % 2 === 0 ? 'Even' : 'odd' }) } function inside function
// const javaRen = javaExercise.map(javaEven)
// console.log(javaRen)

// const JsExcercise3 = (string) => {
//   return (
//     <p>
//       {string}
//     </p>
//   )
// }

// const JsExcercise4 = (listnum) => ( listnum.map((num)=> { // jsex 4 maps the numbers if it even or odd
//     return ( num % 2 === 0 ? <p>{num}{'s'}</p> : <span>{num} </span>

//      )
// })

// </span><p onClick={() => handleRemoveItem(index)} >  {(item.title).map((loop, s) => { return item.num > 2 ? 's' : ' ' })} {item.num}  </p> {/* removes item from list whenever we click it */}
